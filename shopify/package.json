{
  "name": "hope-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "dev-test": "shopify app dev -c shopify.app.insurance-test.toml",
    "push": "shopify app push",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "deploy-test": "shopify app deploy  -c shopify.app.insurance-test.toml",
    "info": "shopify app info"
  },
  "dependencies": {
    "@shopify/cli": "^3.82.0"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "tuyutian",
  "private": true
}